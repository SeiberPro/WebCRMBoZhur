//////////////////////JAVASCRIPT////////////////////////////////

window.innerWidth - ширина внутреннего окна.
window.innerHeight - высота внутреннего окна.

window.pageYOffset - возвращение кол-ва пикселей на которое прокручен документ по вертикали
window.pageXOffset - возвращение кол-ва пикселей на которое прокручен документ по горизонтали

screen - обращение к полному экрану( есть width , height)

obj.offsetTop - отступ элемента от верха родительского элемента
obj.offsetLeft - левый отступ от родителя

obj.clientHeight - получение высоты объекта

event.stopPropagation() - завершение текущего события

obj.value - получить значение из блока

event.key - возвращает значение нажатой кнопки в виде строки
event.keyCode - возваращает числовой идентификатор клавиши, которая была нажата

document.location.href = "http://www.site.ru" - перенос на другую страницу
document.location.replace("http://www.site.ru") - перевод, чтобы в истории не осталась старая страница

String(value) - смена типа данных на строку

Math.random() - функция получения рандомного числа
Math.ceil() - округление вверх
toFixed(2) - округление с указанием точности

arr.splice(начало,количество,замена) - изменение,либо удаление элемента массива
delete arr[] - удаление занчения элемента массива

switch (a) {
  case 3:
    alert( 'Маловато' ) - конструкция case
    break


typeof - узнать тип данных

obj.toLowerCase() - изменение регистра на низкий
obj.toUpperCase() - изменение регистра на высокий

obj.concat() - объединение строки

obj.split() - разделение по указателю

let time = new Date() - получение массива данных, который содержит в себе все временные характеристики
time.getHours() - получение из массива нынешнего часа
time.getDay() - получение номера дня в неделе
time.getMinutes() - получение минут 
time.getSeconds() - получение секунд
time.getMilliseconds() - получение миллисекунд
time.getDate() - получение число месяца от 1 до 31
time.getMonth() - получить месяц от 0 до 11
time.getFullYear() - получить год из 4 цифр

mouseover - обработчик событий (при наведении на элемент)
mouseout - обработчик событий (при сведении мыши с элемента)

drag — срабатывает каждые несколько сотен миллисекунд, пока элемент перетаскивается.

dragstart — срабатывает в момент начала перетаскивания элемента.

dragend — срабатывает в момент, когда перетаскивание элемента завершено.

dragover — срабатывает каждые несколько сотен миллисекунд, пока перетаскиваемый элемент находится над зоной, в которую может быть сброшен.

drop — срабатывает в тот момент, когда элемент будет брошен, если он может быть перемещён в текущую зону.

obj.cloneNode(true) - клонирование элемента


//////////////////////JAVASCRIPT////////////////////////////////

//////////////////////HTML+CSS////////////////////////////////

target="_blank" - открытие ссылки в новой вкладке
title = "text"  - текст, который будет показан при наведении
draggable="true" - атрибут для разрешения перетаскивания

pointer-events: none - запрет на кликабельность по блоку
user-select: none -запрет на выделение объекта

//////////////////////HTML+CSS////////////////////////////////